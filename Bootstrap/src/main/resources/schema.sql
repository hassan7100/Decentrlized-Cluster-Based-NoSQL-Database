CREATE TABLE IF NOT EXISTS users1(
                      username VARCHAR(50) PRIMARY KEY,
                      password VARCHAR(50),
                      email VARCHAR(100),
                      node_number INT,
                      role VARCHAR(50)
);